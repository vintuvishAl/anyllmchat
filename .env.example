# AnyLLMChat Environment Variables
# Copy this file to .env.local for local development

# Convex Configuration (automatically set by `npx convex dev`)
CONVEX_DEPLOYMENT=
EXPO_PUBLIC_CONVEX_URL=

# Note: LLM API Keys are configured in the Convex Dashboard
# Do NOT add API keys here - they are managed securely in Convex
# 
# API Keys configured in Convex Dashboard:
# Currently supported providers:
# - OPENAI_API_KEY (GPT-4, GPT-3.5-turbo, etc.)
# - GOOGLE_GENERATIVE_AI_API_KEY (Gemini Pro, Gemini Flash)
#
# Coming soon:
# - ANTHROPIC_API_KEY (Claude models)
# - Additional providers
#
# To add API keys:
# 1. Run `npx convex dev`
# 2. Go to your Convex Dashboard
# 3. Navigate to Settings > Environment Variables
# 4. Add your API keys there

# App Configuration
EXPO_PUBLIC_APP_ENV=development
